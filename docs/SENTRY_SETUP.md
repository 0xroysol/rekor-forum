# Sentry Error Tracking Setup

## Installation
npm install @sentry/nextjs

## Configuration
1. Create sentry.client.config.ts and sentry.server.config.ts
2. Add NEXT_PUBLIC_SENTRY_DSN to .env.local
3. Wrap next.config.ts with withSentryConfig

## Environment Variables
NEXT_PUBLIC_SENTRY_DSN=your_sentry_dsn_here

This is a placeholder — Sentry integration is prepared but not yet active.
